# 1–2 分钟演示脚本

## 录制前（不进入成片）

1. 连接并授权 Huawei P40 Pro，运行 `python -m bearbless doctor`。
2. 确认 Accessibility Bridge `enabled=true`，网易云已登录且目标歌曲可播放。
3. 连续三次完成相同任务，检查三项隔离指标均为 0。
4. 清理 Dashboard 中与演示无关的旧失败记录，保留最新证据。

## 成片

**0–15 秒：问题和隔离。** 同一镜头拍到用户手持手机、电脑 Shadow Display 和 Dashboard。说明用户拥有 Display 0，Agent 使用虚拟屏。

**15–30 秒：用户继续使用。** 用户在主屏聊天或打字。Dashboard 输入或语音提交：`打开网易云，播放歌曲《银河赴约》`，确认任务边界。

**30–75 秒：并发执行。** Agent 在 Shadow Display 打开网易云、搜索并播放；用户持续操作主屏。Dashboard 展示实时步骤、虚拟屏和零干扰计数。

**75–100 秒：确定性完成。** MediaSession 检测到目标歌曲为播放状态后立即结束，不再回退搜索。展示完成提示和最终虚拟屏。

**100–120 秒：证据。** 聚焦三项指标：Display 0 操作 0、包泄漏 0、IME 违规 0。一句话总结：模型负责决策，确定性运行时负责隔离和完成证明。

若主 Demo 未达到三连胜，改录已经验证过的 Quark/DSB 隔离页面任务，并在答辩中诚实说明音乐输入链路仍在适配；不要现场赌未通过回归的闹钟、美团或 QQ。
